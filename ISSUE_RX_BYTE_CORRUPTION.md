# 专题研究：UDP app RX 方向的线速字节损坏

**状态**: 已收窄边界, **机制未证实** — 待专题研究
**建立日期**: 2026-09-21
**工程**: `D:\repo\ECO\udp_hls_10g` (Kintex-7 XC7K325T, 1G RGMII, 纯 RTL TCP/IP fast-path)
**发现场合**: P5f (UDP app 通路 1Gbps 线速验收) —— 那个验收**已达标**, 本问题是它的副产品

---

## 0. 一句话摘要

板侧 UDP app **接收方向** (PC → 板) 在**接近线速**时出现 **0.05%–0.45% 的载荷字节损坏**:
**收到字节数精确、零丢帧、零 FCS 错、零缓冲溢出, 但部分字节值与图案不符**。
功能仿真**完全零失配**, 且**已排除 RTL 逻辑错**; 坏值产生位置已收窄到 **`mac_rx_64` 的发射搬移寄存器**那一级;
误差率**随会话/温度漂移 5–10 倍**(逻辑错不会有此特征) ⇒ 主假设是**静态时序模型未覆盖的物理效应**,
但**具体寄存器与具体机制均未证实**。

**注**: **发送方向 (板 → PC) 不受影响** —— 928.1 Mbps / 500,007,488 B / `mismatch=0` / `holes=0`。

---

## 1. 现状 (实测数据)

### 1.1 板级实测: UDP app RX (PC → 板, `peer.exe` 发图案, 板侧 `udpapp_*` 计数读 UART)

| 档位 | 帧数 | 送出字节 | 板收 `URB` | `UMM` 失配 | 失配率 | `UOV`/`UPC`/`UPA` |
|---|---|---|---|---|---|---|
| 100 Mbps | 33,968 | 50,000,000 | **50,000,000** | **0** | **0%** | 0/0/0 |
| 400 Mbps | 135,870 | 200,000,000 | **200,000,000** | 216,504 | **0.11%** | 0/0/0 |
| 400 Mbps @512B | 390,625 | 200,000,000 | **200,000,000** | 255,423 | 0.13% | 0/0/0 |
| 700 Mbps | 135,870 | 200,000,000 | **200,000,000** | 426,711 | 0.21% | 0/0/0 |
| ~957 Mbps (不限速) | 339,674 | 500,000,000 | **500,000,000** | 453,530 | 0.09% | 0/0/0 |

独立复核 (第二个 agent, 每轮**重新烧录**保证复位):

| 条件 | `URB` | `UMM` | 失配率 |
|---|---|---|---|
| 100 Mbps, 8.39 MB | 精确 | **0** | 0% |
| 线速, 8.39 MB (**首跑/最冷**) | 精确 | 4,400 | **0.053%** |
| 线速, 8.39 MB (**后续/热**) | 精确 | 24,931–38,133 | **0.30–0.45%** |
| 线速, 80 MB | 精确 | 140,756 | 0.168% |

**`UOV`(缓冲溢出)/`UPC`(FCS 错)/`UPA`(长度不符) 在所有档位恒为 0。**

### 1.2 计数口径 (板侧 UART 状态行, `rtl/app_status_uart.v` 行尾追加的 8 字段)

`URB`=app 收字节 / `UMM`=app 失配字节 / `URF`=app 收帧 / `UOV`=udp_split 溢出丢帧 /
`UPC`=FCS 错 / `UPA`=长度不符 / `UTB`=app 发字节 / `UTF`=app 发帧
(220 → 304 字符; 前 220 字符逐字节不变, `tb/tb_p5_status.v` 与 `tools/gen_stim_p5_app.py` 的 checker 已同步)

### 1.3 误差的分布特征 (关键, 与直觉相反)

- **不是每帧固定字节数**: 1472B 帧实测 **0.8–6.7 字节/帧**, 且**同一档位跨会话差 5–10 倍**。
- **不集中在帧首/帧尾**: 散落在帧身 (0.45% ⇒ 每 ~1472 字节里约 6.7 个坏字节, 约每 25–100 字一个)。
  ⚠️ 曾有"每帧 1~2 字节"的推测, **已被实测否定** —— 那是 1472B 固定帧长下的算术恒等式, 不是机制。
- **按字节缩放而非按帧缩放**: 512B 帧时帧数 ×2.9, 错误率只 +18%。
- **随温度/会话状态漂移**: 首跑最冷 0.053% → 后续热态 0.30–0.45% (5–10×)。

---

## 2. 已确证的边界 (硬结论)

### 2.1 不是 RTL 逻辑错 —— 两个独立仿真门, 线速满载荷, **零失配**

| 门 | 内容 | 结果 |
|---|---|---|
| `sim/udprx/` (`tb/tb_udprx_rate.v`) | 单元级: 直灌 `udp_split` 的 app RX 口 → `app_udp_pattern` 校验器; 200 帧 × 1472B 线速字流 (含 no-IPG 背靠背 / 996 / 512 / 非 8 字节对齐) | **零失配** (每配置 294,400 B) |
| `sim/udprx_chain/` (`tb/tb_udprx_chain.v`) | 全链: 真 GMII 字节流 → `mac_rx_64` → `rx_classify` → `udp_split` → `app_udp_pattern` (真前导/FCS/节拍) | **零失配** (200 帧, mac_rx 无丢帧无 CRC 错) |

**论证强度**: 0.1% 的错误率意味着 600 KB 里应有约 600 个坏字节, 仿真不可能全部漏掉。

### 2.2 坏值产生于 **CRC 输入之后** —— "FCS 盲区"

- FCS 覆盖**整帧**: `rtl/mac_rx_64.v:65` 的 `gmii_rxd` 进 CRC 计算。
- **FCS 通过 ⇒ "收到的字节"是干净的** (线上/PHY 字节错必被 FCS 抓住, 且 `UPC` 实测恒 0)。
- 而 app 看到的载荷有错 ⇒ **坏值出在另一组寄存器**: `rtl/mac_rx_64.v:178-199` 的
  `dline` / `wreg` / `hwreg` 发射搬移寄存器 (以及下游 `u_udp_rx/emit_d`、`u_app_udp/nx_d`)。
- **即**: CRC 算的是*收进来的*字节, 而 emit 出去的是*另一组*寄存器 ⇒ 后者采到陈旧值不会被 FCS 发现。

### 2.3 静态余量确实极小

- 该位流 (P5f 构建): **WNS +0.153 ns / TNS 0.000 / WHS +0.048 ns / 0 失败端点** (全局)。
- **RX 字节携带寄存器的保持余量只有 0.10–0.14 ns**:
  `u_mac_rx/hwreg_reg` 0.104 / `dline_reg` 0.111 / `wreg_reg` 0.122 /
  `u_udp_rx/emit_d_reg` 0.111 / `u_app_udp/nx_d_reg` 0.111 / `cmp_d_reg` 0.137
- ⚠️ **注意**: 这些值**为正** ⇒ **静态时序分析报告"全部满足"**。这恰恰是问题的关键:
  报告说满足, 而实测在满载下出错 ⇒ **是静态模型未覆盖的效应**。

### 2.4 与工程史的关联 (**重要: 不是 P5e 引入的新问题**)

- **P4b-7-P6**: `frame_fifo` 的边存曾"单元 TB 逐拍通过、板级才暴露"(丢 tlast)。
- **P5d 板级记录**: "板侧 HLS echo 会**改写帧尾 1~35 字节**(时序相关, **不丢帧**)" ——
  与本问题的特征 (`URB` 精确 + 载荷错) **高度一致, 很可能是同一现象的不同观测**。
- ⇒ 本问题**早于 P5e 存在**, 只是以前没有"逐字节图案校验 + 精确字节计数"的观测手段而没有暴露。

---

## 3. 已排除的假设 (含排除依据)

| 假设 | 排除依据 |
|---|---|
| RTL 逻辑错 | 两个独立仿真门线速满载荷**零失配** (§2.1) |
| app 校验器吞吐不足 | 400 Mbps 时校验器**62% 空闲**; 且 512B 帧 (帧数 ×2.9) 错误率只 +18% |
| 丢帧 | `URB` **精确等于**送出字节数 (§1.1) |
| 缓冲溢出 | `UOV` 恒 0 |
| 线上 FCS 错 / PHY 接收错 | `UPC` 恒 0; 且 FCS 通过才进 app |
| 长度不符 | `UPA` 恒 0 |
| 帧边界处理 (帧首/尾 keep 掩码) | 失配**散落帧身**, 不集中在首尾 (§1.3) |
| **PC 侧发送端** | 独立 `tshark` + Python 线上逐字节校验: PC 发的图案流 **0/2 MB 坏字节** |
| **板侧发送方向 (TX)** | peer 收板侧 930 Mbps 流: 8.39 MB 与 83.9 MB **均逐字节零失配** |
| 单纯"hold 不足" | 见 §4.2 —— 提升 hold 只降 1.7× **未归零** |

---

## 4. 当前假设 (未证实) 与支持/反对证据

### 4.1 主假设: **静态时序模型未覆盖的物理效应**

候选机制 (三者**尚未被区分开**):

| 候选 | 支持证据 | 反对/未定 |
|---|---|---|
| **片上供电跌落 (droop / IR drop)** | 满载才现; 误差率随温度/会话漂移 5–10×; 帧内字节节拍在所有档位相同, **唯一随档位变的是帧间空隙与累计活动** | 无 XADC/示波器通道可测, 纯推断 |
| **`gmii_clk` 的时钟质量** | `gmii_clk` 来自 **PHY 的恢复时钟 `phy1_rxc`**(不是 MMCM 输出), 抖动/占空比特性差; **整个 RX 通路都在这个域**; **仿真里时钟理想, 这类效应根本不可能覆盖** | 无测量手段 |
| **SSN / 地弹** (高开关活动率) | 同 "满载才现" | 无测量手段 |

### 4.2 反馈信号: **单纯提升 hold 余量只降 1.7×, 未归零**

做的实验 (零 RTL 改动, 只加约束):
- `board/eco_holdfix.xdc`: `set_clock_uncertainty -hold 0.200 [get_clocks gmii_clk]`
- `board/build_p5_holdfix.tcl` (复用 p5_prj, 保留 `Performance_ExtraTimingOpt` 策略)
- 结果 (`p5f_verify/holdfix_timing.rpt`): **WNS +0.183 / WHS +0.007** ⇒ 裸保持余量 ≥0.2 ns (原 0.048)
- **板级交错 A/B** (每轮复位, 8.39 MB 线速):

| 轮次 | ORIG 位流 | HOLDFIX 位流 |
|---|---|---|
| 1 | 0.4546% | 0.1923% |
| 2 | 0.2972% | 0.2447% |
| 平均 | **0.376%** | **0.218%** (~1.7×) |

⇒ **改善但未消除** ⇒ **不全是 hold** ⇒ 指向 setup 侧或时钟质量。

### 4.3 一个**尚未查证的关鍵点** (建议列为第一优先)

**那批寄存器的 `setup` 余量是多少?** 上述报告只给了 **hold** 余量 (§2.3)。
- 满载下**时钟抖动会同时吃掉 setup 和 hold**, 而静态报告只告诉你"最紧的一侧是 hold"。
- 若那批寄存器的 **setup 余量也小**, 则"**抖动导致数据来不及到达**"比"保持不住"更能解释"采到陈旧值"。
- **这一条不查清, 机制无法定案。** (查法: 对 routed DCP 跑 `report_timing -from <这批寄存器> -to <下游>` / `-max_paths`, 只读, 免费)

---

## 5. 做过的诊断 (完整清单, 含产物路径)

### 5.1 仿真 (均零失配)
- `sim/udprx/` — 单元级线速门 (`tb/tb_udprx_rate.v`)
- `sim/udprx_chain/` — 全链真 GMII 门 (`tb/tb_udprx_chain.v`)
- `sim/p5e_rate/` — P5f 验收门 (`run_tb_rate.bat` + `tb/tb_app_udp_rate.v`)

### 5.2 板级测量 (全部用 C++ `tools/cpp_peer/peer.exe`, 用户要求速率测试不用 Python)
- 各档位扫描 (§1.1) — peer `--udp-send-pattern` + 板侧 UART 读 `udpapp_*`
- 交错 A/B (§4.2) — 每轮重新烧录复位
- 独立线上校验 — `tools/pc_wire_pattern_check.py` (tshark + Python, PC 侧 0/2MB 坏字节)
- 板侧 TX 方向对照 — peer `--udp-rx-only`, 8.39/83.9 MB 零失配

### 5.3 时序分析 (只读)
- `p5f_verify/p5e_timing_routed.rpt` — 逐 v2025.2 重跑 routed DCP 取 P5f 位流的时序
- 那批寄存器的 hold 余量 (§2.3) — `report_timing -to <reg>`

### 5.4 约束实验
- `board/eco_holdfix.xdc` + `board/build_p5_holdfix.tcl` (§4.2)

### 5.5 观测手段的建设 (本次新增, 后续研究直接可用)
- `rtl/app_status_uart.v`: UART 状态行追加 8 字段 (`URB/UMM/URF/UOV/UPC/UPA/UTB/UTF`), 220→304 字符
- 同步更新: `tb/tb_p5_status.v` + `tools/gen_stim_p5_app.py` 的 checker (整行逐字节比对, status 门通过)

---

## 6. ⚠️ 复现方法与**方法学陷阱** (必读)

### 6.1 铁律: **每次测量前必须重新烧录位流**

- app 的 RX LFSR **永不重同步**, 且 `UMM` 是**粘滞计数**。
- 若不复位就连续测量, 图案相位会错开 ⇒ 读到 **恰 99.609%–99.610% = 255/256** 的**假失配**。
  (实测: 12 GB 流量的假数据 = 255/256 × 100%。)
- **顺带的反证价值**: 这个"255/256"恰好证明**校验器逐字节比对是精确的、无跳字/重字**。

### 6.2 复现命令骨架 (C++, 不用 Python 测速率)

```bash
# 1) 复位 = 重新烧录
cmd //c 'D:\repo\ECO\udp_hls_10g\board\run_program_p5.bat'

# 2) 发图案 (UDP, 端口必须 8081↔8081 —— 8080 是 HLS echo 的)
tools/cpp_peer/peer.exe --iface '\Device\NPF_{...}' \
    --src-mac <PC_MAC> --sport 8081 --dport 8081 \
    --udp-send-pattern <N> --udp-paylen 1472 --rate-mbps <0=不限速>

# 3) 读板侧计数 (Python 只用于读串口/解析)
python tools/board_p5b_check.py --port COM9
#    看 URB/UMM/UOV/UPC/UPA
```

### 6.3 其它环境要点
- 串口 = **COM9** (板载 CH340E, 9600-8N1)
- 需 `fw_block.ps1` 屏蔽内核对板 IP 的 RST (T6 报告: 用后已 `-Remove`)
- peer 的 **flush 路径上限 ~191k 帧/s** (≈1.1 Gbps 载荷): 若结果接近该量级, 必须区分"板子发不动"与"工具收不动"
- peer 同进程双向量速**不可行** (RX 线程在 `run_send` 期间几乎不工作) ⇒ 用"检查器 + 触发进程分离 + 源端口错开"

---

## 7. 建议的下一步 (按性价比排序)

| # | 实验 | 成本 | 能回答什么 |
|---|---|---|---|
| **1** | **查那批寄存器的 `setup` 余量** (§4.3) — 对 routed DCP 只读 `report_timing` | **0** (只读) | 是否 setup 侧也紧 ⇒ 能否把"抖动吃掉 setup"这一支定下来 |
| **2** | **给 `app_udp_pattern` 加"首个失配位置 + 坏值快照"寄存器** — 记录字内 bit/byte lane、帧内偏移、当时的 `tvalid/tready` 相位 | 小改 + 重建 (~18 min) | **最有价值**: 区分"采到陈旧值"(单 bit/lane 错) vs "整字错位/重复"(指针类), 并定位到**具体寄存器** |
| 3 | **hold uncertainty 扫描** (0.3 / 0.4 ns) 与**关掉 TX 全速**做活动源 A/B | 小 (约束 + 重建) | hold 侧的敏感性曲线; "活动源"是否就是 droop 的来源 |
| 4 | **ILA 抓 `rx_tdata`/`emit_d`** 在失配时刻的波形 | 中 (需 ILA + 触发条件) | 信号级证据 |
| 5 | **测量 `gmii_clk` 质量 / 片上电压** | 高 (需示波器 / XADC 通道) | §4.1 三个候选机制的区分 |

**推荐顺序**: 1 → 2 → 3。**第 1 步免费且可能直接改变方向; 第 2 步一次实验就能把"哪一级、哪一类错"钉死。**

---

## 8. 相关文件索引

| 类别 | 路径 |
|---|---|
| **嫌疑模块** | `rtl/mac_rx_64.v` (`:65` CRC 输入 / `:178-199` 发射搬移寄存器) · `rtl/udp_rx.v` · `rtl/udp_split.v` · `rtl/app_udp_pattern.v` |
| **观测** | `rtl/app_status_uart.v` (UART 状态行 304 字符) · `tools/board_p5b_check.py` |
| **仿真门** | `sim/udprx/` · `sim/udprx_chain/` · `sim/p5e_rate/` |
| **时序** | `p5f_verify/p5e_timing_routed.rpt` · `p5f_verify/holdfix_timing.rpt` |
| **约束实验** | `board/eco_holdfix.xdc` · `board/build_p5_holdfix.tcl` |
| **位流** | `vivado_prj/p5_prj.runs/impl_1/wrapper_p4.bit` (现为 HOLDFIX 版) · `.../wrapper_p4_orig_whs048.bit` (回滚版) |
| **工具** | `tools/cpp_peer/peer.cpp` (UDP 模式) · `tools/pc_wire_pattern_check.py` |
| **历史同类现象** | `PORT_NOTES.md` 的 P5d 段("帧尾 1~35 字节被改写") · P4b-7-P6 的 `frame_fifo` 边存丢 tlast |

---

## 9. 工程背景 (给新 session 的最小上下文)

- **平台**: Kintex-7 XC7K325T, **1G RGMII**, 纯 RTL TCP/IP fast-path (64-bit 字流 @125MHz, 每级 II=1) + HLS 慢路径。
- **P0–P5e 已完成并推送** (最新提交见 `git log`); **P6 (10G) 经用户裁决不做**。
- **本问题的观测入口**: P5e 新增的 UDP app 通路 —— `rx_classify.slow → udp_split → app UDP RX 口`, 板侧演示 app `app_udp_pattern` 逐字节校验图案 (xorshift64, seed `0x9E3779B97F4A7C15`, **先取后推进**, 与 `peer.exe --udp-*` 一致)。
- **速率测试一律用 C++** (`tools/cpp_peer/peer.exe`); Python 只用于读串口 / 解析判据 / 生成 TB 激励。
- **风险分级**: 本问题在 **100 Mbps 下为零**, 但**行情数据 0.2% 误码是致命的** ⇒ 若要用于生产, 必须先定案。
